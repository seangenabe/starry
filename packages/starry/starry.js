'use strict'

module.exports = {
  asyncAll: require('starry.async-all'),
  asyncRace: require('starry.async-race'),
  count: require('starry.count'),
  every: require('starry.every'),
  filter: require('starry.filter')
}
